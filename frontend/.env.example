# URL base da API do backend.
# Em desenvolvimento com Vite dev server, deixe vazio (o proxy cuida disso).
# Em produção/Docker, defina o endereço completo do backend, ex:
#   VITE_API_URL=http://localhost:3000
VITE_API_URL=
